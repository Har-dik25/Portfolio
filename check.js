import puppeteer from 'puppeteer'; (async () => { try { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('LOG:', msg.text(), msg.type())); page.on('pageerror', err => console.error('ERR:', err.toString())); await page.goto('http://localhost:5175'); await new Promise(r => setTimeout(r, 2000)); const html = await page.content(); console.log('BODY:', html.substring(0, 500)); await browser.close(); } catch(e) { console.log(e); } })();
